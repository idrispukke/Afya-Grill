# Copie este arquivo para ".env" (na raiz do projeto) e preencha com os
# dados do seu projeto Supabase: Project Settings → Database → Connection
# string (aba "URI").
DATABASE_URL=postgresql://postgres:[SUA-SENHA]@db.xxxxxxxxxxxx.supabase.co:5432/postgres
